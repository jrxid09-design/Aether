const AgentPhase = require("../runtime/agentPhase");

class ReflectionEngine extends AgentPhase {
  async run(state) {
    // Belum melakukan apa-apa.
    // Nanti di sini kita akan memilih memory yang perlu disimpan.
    return state;
  }
}

module.exports = ReflectionEngine;