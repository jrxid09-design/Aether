"use strict";

// Manager media ingress is a runtime-composition detail.  The public module
// intentionally offers no constructor or caller-selectable media binding.
module.exports = Object.freeze({});
